self.addEventListener('push',event=>{const data=event.data?event.data.json():{title:'Ledger',body:'Time for your weigh-in',url:'/log'};event.waitUntil(self.registration.showNotification(data.title,{body:data.body,data:{url:data.url||'/log'}}));});
self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{const open=list.find(c=>new URL(c.url).origin===self.location.origin);return open?open.focus().then(()=>open.navigate(event.notification.data.url)):clients.openWindow(event.notification.data.url);}));});
